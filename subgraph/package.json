{
  "name": "dao-delegate-registry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service maheshmurthy/dao-delegate-registry",
    "create-local": "graph create --node http://localhost:8020/ mmurthy/dao-delegate-registry",
    "remove-local": "graph remove --node http://localhost:8020/ mmurthy/dao-delegate-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mmurthy/dao-delegate-registry",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.36.1",
    "@graphprotocol/graph-ts": "0.28.1",
    "assemblyscript-json": "^1.1.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
